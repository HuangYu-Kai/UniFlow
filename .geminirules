# Gemini Code Assist / AI Agent 專案開發規範 (Uban Flutter 端)

## ⚠️ 絕對優先事項 ⚠️
在提供任何程式碼修改前，**必須先完整閱讀 `README.md`**。

## 專案邊界與紅線

### 架構
- **前端**：Flutter (Dart)，使用 Provider 狀態管理
- **正式後端**：`uban-api/` (FastAPI + python-socketio[asgi])
- **前端不使用 Flutter Web**，僅支援 Android/iOS 原生

### 雙軌制網路架構 (禁止合併!)
- **信令 (第一軌)**：Tailscale Funnel (TCP/WSS) → `https://localhost-0.tail5abf5e.ts.net`
- **媒體中繼 (第二軌)**：Oracle Cloud Coturn (UDP) → `turn:152.69.196.5:3478`
- **原因**：Tailscale 免費提供 HTTPS (開鏡頭必須)，但不支援 UDP (影像需要)

### 通訊架構
- **信令**：Socket.IO（python-socketio ASGI 模式）← 走 Tailscale (TCP/WSS)
- **視訊**：WebRTC P2P + STUN + Oracle coturn TURN ← 走 Oracle Cloud (UDP)
- **推播**：Firebase FCM（背景喚醒 + CallKit 來電）
- **備援**：Socket.IO 即時 → FCM 推播 → Cold Start 三層

### 禁止事項
- ❌ 在代碼中硬編碼伺服器 IP（使用 `--dart-define`）
- ❌ 在 Signaling 以外的地方建立 Socket 連線（必須 Singleton）
- ❌ 在長輩端直接啟動 `VideoCallScreen`（必須透過 `ElderScreen`）
- ❌ 在 `openUserMedia` 前呼叫 `createOffer`
- ❌ 在 `setRemoteDescription` 前直接 `addIceCandidate`（必須排隊暫存）
- ❌ 將信令伺服器與 TURN 伺服器合併（雙軌制，禁止合併）

## Socket.IO / WebRTC 開發協議

### 通話流程 (Critical Path)
```
1. 家屬 emit call-request → 2. 長輩收到 → 彈出 CallKit
3. 長輩接聽 emit call-accept(帶自己的 sid)
4. 家屬收到 call-accept → 立刻 createOffer(targetId: 長輩sid)
5. 長輩收到 offer → setRemoteDescription → createAnswer → emit answer
6. 雙方交換 ice-candidate
7. P2P 建立成功
```

### 必讀檔案
| 檔案 | 用途 | 讀取優先級 |
|------|------|-----------|
| `lib/services/signaling.dart` | Socket.IO + WebRTC 信令（Singleton） | 🔴 最高 |
| `lib/main.dart` | App 入口 + FCM + CallKit 全域監聽 | 🔴 最高 |
| `lib/globals.dart` | `pendingAcceptedCall` 全域狀態 | 🟡 中 |
| `lib/screens/elder_screen.dart` | 長輩端通話（含 CCTV/緊急模式） | 🟡 中 |
| `lib/screens/video_call_screen.dart` | 家屬端通話 UI | 🟡 中 |
| `test_call_simulator.py` | 信令自動化測試腳本 | 🟢 低 |

### ICE 伺服器配置 (雙軌制)
```dart
// signaling.dart 內建 Oracle Cloud TURN 預設值：
// TURN URI: turn:152.69.196.5:3478 (Oracle Cloud, 日本大阪)
// TURN User: uban / TURN Pass: 115207
// 也可透過 --dart-define 覆蓋：
// --dart-define=TURN_SERVER=152.69.196.5:3478
// --dart-define=TURN_USER=uban
// --dart-define=TURN_PASS=115207
```

### 角色差異
| | 長輩端 | 家屬端 |
|--|--------|--------|
| 入口 | `ElderScreen` | `VideoCallScreen` |
| Socket 角色 | `elder` | `family` |
| 視訊 UI | 全螢幕沉浸式 + 大按鈕 | 標準控制列 |
| 語音模式 | `isVideoCall: false` 不啟動攝影機 | 預設視訊 |
| 來電處理 | CallKit → `pendingAcceptedCall` → `ElderScreen` | Dialog in `FamilyMainScreen` |

## 重要檔案索引

### 前端核心
- `lib/services/signaling.dart` — Socket.IO + WebRTC 信令
- `lib/services/api_service.dart` — REST API 呼叫
- `lib/services/elder_manager.dart` — 長輩資料管理
- `lib/main.dart` — App 入口 + FCM + CallKit
- `lib/globals.dart` — 全域狀態變數

### 前端畫面
- `lib/screens/elder_tabs/elder_home_tab.dart` — 長輩首頁（聯絡家人入口）
- `lib/screens/elder_screen.dart` — 長輩端通話
- `lib/screens/video_call_screen.dart` — 家屬端通話
- `lib/screens/family_main_screen.dart` — 家屬主畫面（來電監聯）
- `lib/screens/device_selection_screen.dart` — 裝置選擇

### 測試工具
- `test_call_simulator.py` — Socket.IO 信令測試
- `webrtc_test.html` — 瀏覽器版 WebRTC 測試（含 TURN 驗證）

## 長輩友善 UI 開發規範 (Elder-Friendly UX)

### 色彩與對比
- **高對比度**：背影與文字必須符合 WCAG AAA 標準或至少 AA。
- **色彩意義**：不要僅依靠色彩來傳達資訊（例如：不要只用紅色標示錯誤，應加上圖示或文字）。

### 字體與排版
- **最小字體**：主要內容不得小於 24pt，次要資訊不得小於 18pt。
- **行距/字距**：適度加大（至少 1.2 倍行高），方便閱讀。
- **加重重點**：重要關鍵字使用 `FontWeight.bold`。

### 互動與觸控
- **點擊區域**：所有按鈕或互動元素的 Hit Test 區域至少為 60x60dp。
- **操作回饋**：所有點擊必須有視覺（如亮起）與觸覺（震動）回饋。
- **單一路徑**：盡量減少多層嵌套選單，保持單一、線性的操作流程。

### AI 呈現
- **大頭照與身分**：AI 的身分是動態的（由資料庫 `ai_persona` 提供），UI 應能適配不同角色的頭像。
- **語音同步**：AI 說話時，畫面上應有明顯的波形或波紋視覺特效，代表「我正在聽」或「我正在說」。
